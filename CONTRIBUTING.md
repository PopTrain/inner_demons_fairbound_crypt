This project uses Vite, Capacitor, Electron, and PixiJS.
These frameworks must be installed on your local repository (alongside NodeJS) before attempting to make any changes to the project's code.

npm run build

npm run sync
npm run dev
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crypt.innerdemons',
  appName: 'Inner Demons Fairbound Crypt',
  webDir: 'dist'
};

export default config;
